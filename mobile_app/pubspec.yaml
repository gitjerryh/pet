name: mech_dog_controller
description: 机械狗控制移动应用
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^0.13.3
  webview_flutter: ^3.0.0
  control_pad: ^1.1.1
  permission_handler: ^8.3.0
  wakelock: ^0.6.1
  shared_preferences: ^2.0.9
  flutter_joystick: ^0.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/ 